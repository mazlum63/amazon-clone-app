/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.nicepng.com', 'fakestoreapi.com', 'm.media-amazon.com']
  }
}

module.exports = nextConfig
